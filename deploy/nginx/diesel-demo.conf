server {
    listen 80;
    listen [::]:80;
    server_name 111.228.50.85 diesel.jamesky.site;

    # File/image uploads must never traverse a plaintext alternate origin.
    # Preserve the path and query while converging on the canonical TLS host.
    return 301 https://jamesky.site$request_uri;
}
